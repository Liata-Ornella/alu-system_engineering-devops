# SSH Project

This directory contains Bash scripts and configuration files for managing SSH keys and connections in Ubuntu.

## Project Files

### 0-use_a_private_key
A Bash script that connects to a server using the private key `~/.ssh/school` with the user `ubuntu`.

**Usage:**
```bash
./0-use_a_private_key
./1-create_ssh_key_pair
cat > README.md <<'EOF'
